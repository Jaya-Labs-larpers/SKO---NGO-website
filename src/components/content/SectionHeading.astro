---
interface Props {
  title: string;
  intro?: string | null;
  introLang?: string | undefined;
  as?: 'h2' | 'h3';
}

const { title, intro, introLang, as: Tag = 'h2' }: Props = Astro.props;
---

<div class="max-w-[640px]">
  <div class="rule-accent"></div>
  <Tag
    class="mt-3 text-[length:var(--text-h2)] leading-[var(--text-h2--line-height)] tracking-[var(--text-h2--letter-spacing)]"
  >
    {title}
  </Tag>
  {
    intro && (
      <p class="mt-3.5 text-[length:var(--text-lead)] leading-[1.6] text-[color:var(--color-ink-400)]" lang={introLang}>
        {intro}
      </p>
    )
  }
</div>
