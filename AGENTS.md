# Agent instructions

## Comments

Default to no comments. Well-named code should explain itself.

Only add a comment when it captures something the code can't: a non-obvious
reason, a workaround for a specific bug, a constraint that would surprise a
reader. Never comment what the code already says.
