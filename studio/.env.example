# Sanity Studio — local development
# Copy to .env (git-ignored). These are not secrets: a project id is public.

SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Optional: the subdomain used by `npm run deploy` -> <hostname>.sanity.studio
SANITY_STUDIO_HOSTNAME=
